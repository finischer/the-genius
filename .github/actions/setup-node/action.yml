name: 'Setup Bun'
description: 'Extract Node.js version from package.json and setup Bun with caching'
outputs:
  node-version:
    description: 'The detected Node.js version'
    value: ${{ steps.extract_node_version.outputs.NODE_VERSION }}
runs:
  using: 'composite'
  steps:
    - name: Extract Node.js version
      id: extract_node_version
      shell: bash
      run: |
        NODE_VERSION=$(jq -r '.engines.node' package.json)
        echo "NODE_VERSION=$NODE_VERSION" >> $GITHUB_OUTPUT
        echo "📦 Detected Node.js version: $NODE_VERSION"

    - name: Set up Bun
      uses: oven-sh/setup-bun@v2
      with:
        bun-version: latest
